<UserControl x:Class="RGBtoGray.View.ReadConvertedImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:viewModel="clr-namespace:RGBtoGray.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.DataContext>
		<viewModel:ReadConvertedImage/>
	</UserControl.DataContext>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="20" />
			<RowDefinition Height="20"/>
			<RowDefinition Height="30" />
			<RowDefinition Height="*" />
			<RowDefinition Height="40" />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="2" Text="After conversion:" Margin="10,0,0,0"/>
		<Image Grid.Row="3" Source="{Binding ConvertedImage, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,10,0"/>
		<Button Content="Convert"  Grid.Row="4" VerticalAlignment="Center" Width="75" Command="{Binding ConvertCommand}"/>
		<TextBlock Grid.Row="5" Text="{Binding ConvertionTime, UpdateSourceTrigger=PropertyChanged}" Margin="10,0,0,0"/>
	</Grid>
</UserControl>
